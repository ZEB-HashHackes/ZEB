import express from "express";
const router = express.Router();
// Placeholder for activity routes
router.get("/", (req, res) => {
    res.json({ message: "Activity routes placeholder" });
});
export default router;
//# sourceMappingURL=activity.routes.js.map