[package]
name = "zeb"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["lib", "cdylib"]
doctest = false

[dependencies]
soroban-sdk = "23.3"  # replace with latest stable version

[dev-dependencies]
soroban-sdk = { version = "23.3", features = ["testutils"] }
